[package]
name = "sa-mod-manager"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.31"
rfd = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
